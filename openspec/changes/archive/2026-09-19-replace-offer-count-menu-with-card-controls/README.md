# replace-offer-count-menu-with-card-controls

Remove the shared offer-count dropdown; per-card steppers become the only count control, and the card header becomes the fold trigger with an accent chevron.
